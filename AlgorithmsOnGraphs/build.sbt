name := "AlgorithmsOnGraphs"

version := "0.1"

scalaVersion := "2.12.7"

scalaSource in Compile := baseDirectory.value / "src" / "main" / "java"
scalaSource in Compile := baseDirectory.value / "src" / "main" / "scala"